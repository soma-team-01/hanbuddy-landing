.superpowers/
.omo/

# Raw local photos are not production assets. Keep only processed WebP assets deployable.
# assets/raw/ holds all local originals (never tracked, never deployed).
assets/raw/
assets/**/*.jpg
assets/**/*.jpeg
assets/**/*.JPG
assets/**/*.JPEG
.vercel
.DS_Store
