# HanBuddy Landing
- Buildless static public recruitment/promotional page; production surface is `index.html` plus approved `assets/*.webp`.
- Primary audience: international guests; local-buddy recruitment stays a one-line final-CTA note.
- Public proof uses only approved Run 1 WebP derivatives, completed-operation facts, and the approved testimonial.
- Read `mem:tech_stack` for runtime/deployment constraints, `mem:conventions` for public-copy and asset guardrails, `mem:suggested_commands` for daily commands, and `mem:task_completion` for verification.